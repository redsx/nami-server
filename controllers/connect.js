const User = require('../models/user');
const StatusMap = require('../constants/status');
const redisCli = require('../dbs/db_redis');

module.exports = {
    async autoDisConnect(socket) {
        
    },
}